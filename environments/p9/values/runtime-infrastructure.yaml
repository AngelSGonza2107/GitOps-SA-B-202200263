postgresql:
  enabled: true
  primary:
    podAnnotations:
      backup.velero.io/backup-volumes: data
rabbitmq:
  enabled: true
  podAnnotations:
    backup.velero.io/backup-volumes: data
ingress-nginx:
  enabled: true
  controller:
    watchNamespace: sa-p9

